auto ok = [&](int mid) 
{
    // return true/false;
};

int lft = 0, rgt = n, mid, ans = 0;
while(lft<=rgt)
{
    mid = lft + (rgt-lft)/2;
    // if(ok(mid))
    // {
    //     ans = mid;
    //     lft = mid+1;
    // }
    // else
    // {
    //     rgt = mid-1;
    // }
}